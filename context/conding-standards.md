## Commit conventions

- Small, focused commits. A commit that changes both a business rule and
  unrelated styling should be split.
- Commit messages: imperative mood, e.g. `Add close-session proration
logic`, not `Added` or `Adds`.
